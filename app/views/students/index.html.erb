<div>
  <% @students.each do |student| %>
    <div><%= student.first_name + " " + student.last_name %></div>
  <% end %>
</div>
<% @students.each do |stud| %>
  <div><%= link_to stud.to_s, student_path(stud) %></div>
<% end %>
